# Shared portfolio deps. Streamlit Community Cloud installs this file from the
# repo root by default. Keep it lean — do not add jupyter, torch, or
# sentence-transformers here (they blow Cloud memory/boot time).
#
# Optional dense / hybrid retrieval (local only):
#   pip install -r projects/local-llm-demo/requirements-hybrid.txt
#
# Local notebooks (not for Cloud):
#   pip install jupyter
pandas>=2.0
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.4
numpy>=1.26
streamlit>=1.32
